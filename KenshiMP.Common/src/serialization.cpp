// ES: Unidad de compilación de la serialización de mensajes. Los structs de mensajes
//     (messages.h) y el lector/escritor de paquetes (protocol.h) son solo cabecera;
//     el fichero está vacío y se mantiene como punto de extensión.
// EN: Message serialization compilation unit. Message structs (messages.h) and the
//     packet reader/writer (protocol.h) are header-only; this file is empty and kept
//     as an extension point.
#include "kmp/protocol.h"
#include "kmp/messages.h"

namespace kmp {

} // namespace kmp
