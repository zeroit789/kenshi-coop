// ES: Unidad de compilación de compression.h. Toda la lógica es inline en la cabecera;
//     este fichero existe solo para que la librería estática tenga una fuente asociada.
// EN: Compilation unit for compression.h. All logic is inline in the header;
//     this file only exists so the static library has a matching source.
#include "kmp/compression.h"

namespace kmp {

} // namespace kmp
