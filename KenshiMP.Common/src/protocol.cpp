#include "kmp/protocol.h"

// Protocol implementation - mostly header-only via PacketReader/PacketWriter.
// This file exists for future extensions (packet validation, checksums, etc.)

namespace kmp {

} // namespace kmp
