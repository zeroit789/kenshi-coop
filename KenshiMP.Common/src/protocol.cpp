// ES: Unidad de compilación del protocolo. La lectura/escritura de paquetes vive en
//     protocol.h (PacketReader/PacketWriter, solo cabecera); aquí no hay código todavía.
// EN: Protocol compilation unit. Packet reading/writing lives in protocol.h
//     (PacketReader/PacketWriter, header-only); there is no code here yet.
#include "kmp/protocol.h"

// ES: Implementación del protocolo — casi todo es solo cabecera vía PacketReader/PacketWriter.
//     Este fichero queda para futuras extensiones (validación de paquetes, checksums, etc.).
// EN: Protocol implementation - mostly header-only via PacketReader/PacketWriter.
//     This file exists for future extensions (packet validation, checksums, etc.)

namespace kmp {

} // namespace kmp
